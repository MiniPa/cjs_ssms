# =======================
# Shiro INI configuration -- spring
# =======================

[main]
# Objects and their properties are defined here,
# Such as the securityManager, Realms and anything
# else needed to build the SecurityManager
# AtLeastOneSuccessfulStrategy FirstSuccessfulStrategy AllSuccessfulStrategy

# authcStrategy = org.apache.shiro.authc.pam.FirstSuccessfulStrategy
# securityManager.authenticator.authenticationStrategy = $authcStrategy

# DefaultFilter: anon,authc,authBasic,logout,noSessionCreation,perms
#               port,rest,roles,ssl,user

# sysUserRealm = SysUserRealm
# securityManager.realms = $sysUserRealm

[users]
# The 'users' section is for simple deployments
# when you only need a small number of statically-defined
# set of User accounts.

[roles]
# The 'roles' section is for simple deployments
# when you only need a small number of statically-defined
# roles.

[urls]
# The 'urls' section is used for url-based security
# in web applications.  We'll discuss this section in the
# Web documentation





;
;# =============================================================================
;# Tutorial INI configuration -- Application
;#
;# Usernames/passwords are based on the classic Mel Brooks' film "Spaceballs" :)
;# =============================================================================
;
;# -----------------------------------------------------------------------------
;# Users and their (optional) assigned roles
;# username = password, role1, role2, ..., roleN
;# -----------------------------------------------------------------------------
;[users]
;admin = admin, admin
;root = secret, admin
;guest = guest, guest
;presidentskroob = 12345, president
;darkhelmet = ludicrousspeed, darklord, schwartz
;lonestarr = vespa, goodguy, schwartz
;
;# -----------------------------------------------------------------------------
;# Roles with assigned permissions
;# roleName = perm1, perm2, ..., permN
;# -----------------------------------------------------------------------------
;[roles]
;admin = *
;schwartz = lightsaber:*
;goodguy = winnebago:drive:eagle5


